[Webapp]
Login Username: demo


[Testing]
[Prerequisites]
1. Ensure you have every "index.html", "mock_return_server", "test_mug_return_latency", and the Assets folder in the same directory.

[Instructions]
1. In Command Prompt, run "mock_return_server" and wait until "Press CTRL+C to quit" appears.
2. In a separate Command Prompt terminal, run test_mug_return_latency.
